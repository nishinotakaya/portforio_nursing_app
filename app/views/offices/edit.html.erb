<h1>拠点情報編集</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(model: @office, url: office_path(@office), local: true, method: :patch) do |f| %>
    <%= render 'shared/error_messages',  object: @office %>
    
    <%= f.label :拠点名, class: "label-#{yield(:class_text)}" %>
    <%= f.text_field :office_name, class: "form-control" %>
    
    <%= f.label :拠点番号, class: "label-#{yield(:class_text)}" %>
    <%= f.text_field :office_number, class: "form-control" %>
    
    <%= f.label :拠点の種類, class: "label-#{yield(:class_text)}" %>
    <%= f.text_field :office_modality, class: "form-control" %>
    
    
    <%= f.submit :追加, class: "btn btn-primary btn-block btn-#{yield(:class_text)}" %>
    <% end %>
  </div>
</div>