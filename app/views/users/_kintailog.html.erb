<div class="modal-dialog modal-lg modal-dialog-center">
  <div class="modal-content">
    <div class="modal-header" , style="border:none">
      <button type="button" class="close" data-dismiss="modal" aria-label="閉じる">
        <span aria-hidden="true">
          <font style="vertical-align: inherit;">×
          </font>
        </span>
        </button>
      <h1 class="modal-title" , style="text-align:center">勤勉修正ログ</h1>
      <div class="modal-body">
        <table class="apporoved-log-table-header table-bordered table-striped table-condensed">
  　　　   　<form method="post" action="example.cgi">
  　　　    　<p><input type="reset" value="リセット"></p>
           <tbody>
              <tr>
                <td>年</td>
                <td><%= form_with(model: @user,url: users_path(@user), local: true) do |f| %>
                    <%= f.date_select(:date_separator, discard_month:true) %> 
                    <% end %></td>
              <tr>
                <td>月</td>
                <td><%= form_with(model: @user,url: users_path(@user), local: true) do |f| %>
                    <%= f.date_select(:use_month_numbers, discard_year:true, discard_day:true) %>
                    <% end %></td>  
              </tr>
              </tr>
            </tbody>
          </form>
        </table>  
        <table class="apporoved-log-table table-bordered table-striped table-condensed">
          <thead>
            <tr>
              <th>日付</th>
              <th>変更前出社時間</th>
              <th>変更前退社時間</th>
              <th>変更後出社時間</th>
              <th>変更後退社時間</th>
              <th>指示者</th>
              <th>承認者</th>
            </tr>
          </thead>
          
          <tbody>
            <tr>
              <td></td>
              <td></td>
              <td></td>
              <td></td>
              <td></td>
              <td></td>
              <td></td>
            </tr>
          </tbody>
          
          <tbody>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
          </tbody>
        </table>  
      </div>  
    </div>
  </div>
</div>  
   %>